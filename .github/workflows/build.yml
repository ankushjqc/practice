name: CI build

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - name: make clean
      run: make clean
    - name: make 
      run: make 

    - name: Deploy
      run: echo "Step Deploy called"
